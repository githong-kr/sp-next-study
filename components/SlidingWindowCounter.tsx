import { RateLimitingData } from './FixedWindowCounter';

export default function SlidingWindowCounter({ apiPath }: RateLimitingData) {
  return (
    <div>
      <h1>SlidingWindowCounter</h1>
    </div>
  );
}
